// Currency service - placeholder for future exchange rate integration
export const convertCurrency = async (
  amount: number,
  fromCurrency: string,
  toCurrency: string
): Promise<number> => {
  // Future implementation for exchange rate conversion
  // For now, return same amount
  return amount;
};
